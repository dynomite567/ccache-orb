version: 2.1

description: >
  Cache and speedup C/C++ project builds within CircleCI jobs using ccache on Linux.

display:
  source_url: "https://github.com/dynomite567/ccache-orb"

# If your orb requires other orbs, you can import them like this. Otherwise remove the "orbs" stanza.
# orbs:
#  hello: circleci/hello-build@0.0.5
